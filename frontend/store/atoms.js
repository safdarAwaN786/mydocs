import { atom } from 'jotai';

export const currentDoc = atom(null);
export const allDocs = atom(null);
export const showComments = atom(true);  
export const wholeLoading = atom(true);  
export const clickedComment = atom(null);
export const tocHeadings = atom([]);
export const allPages = atom([]);







